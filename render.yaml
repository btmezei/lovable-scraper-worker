services:
  - type: web
    name: lovable-scraper
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /healthz
    envVars:
      - key: SCRAPER_TOKEN
        sync: false
      - key: BRIGHTDATA_SCRAPING_BROWSER_WSS
        sync: false
